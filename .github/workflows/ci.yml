name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v7
      - name: Build
        run: swift build
      - name: Test
        run: swift test
      - name: Check account list layout
        run: ./scripts/check-account-list-layout.sh
      - name: Bundle app
        run: ./scripts/build-app.sh
